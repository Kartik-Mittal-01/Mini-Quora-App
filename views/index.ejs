<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>POSTS</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>QUORA POSTS</h1>
    <div class="posts-container">
        <% for(post of posts) { %>
            <div class="post">
                <h3 class="user">@UserName: <%= post.username %></h3>
                <h4 class="content">Posts: <%= post.content %></h4>
                <div class="post-links">
                    <a href="http://localhost:8081/posts/<%= post.id %>" class="detail-link">See In Detail</a>
                    <a href="http://localhost:8081/posts/<%= post.id %>/edit" class="edit-link">Edit</a>
                </div>
                <div class="post-form">
                    <form action="/posts/<%= post.id %>?_method=DELETE" method="post">
                        <button>Delete Post</button>
                    </form>
                </div>
            </div>
        <% } %>
    </div>

    <hr>
    <a href="http://localhost:8081/posts/new">CREATE NEW POST</a>
</body>
</html>
